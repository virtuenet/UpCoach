# Google OAuth Configuration
# Copy this file to .env and fill in your actual values

# Google OAuth Client ID (from Google Cloud Console)
# Required: This is the main client ID for your application
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google OAuth Client Secret (optional for mobile apps)
# Only needed for server-side OAuth flows
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Mobile-specific Google Client ID (optional)
# Use this if you have a separate OAuth client for mobile apps
GOOGLE_MOBILE_CLIENT_ID=your-mobile-client-id.apps.googleusercontent.com

# Web-specific Google Client ID (optional)
# Use this if you have a separate OAuth client for web apps
GOOGLE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com

# Email verification requirement
# Set to 'true' to require email verification from Google
REQUIRE_EMAIL_VERIFICATION=false

# Supabase Configuration (for user synchronization)
# These should already be in your main .env file
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# JWT Configuration (should already be in main .env)
JWT_SECRET=your-jwt-secret-key
JWT_REFRESH_SECRET=your-jwt-refresh-secret-key
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=30d

# Redis Configuration (for session management)
REDIS_URL=redis://localhost:6379

# Security Settings
BCRYPT_ROUNDS=14

# Setting up Google OAuth:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable Google+ API
# 4. Go to Credentials > Create Credentials > OAuth 2.0 Client ID
# 5. For Mobile: Choose "iOS" or "Android" application type
# 6. For Web: Choose "Web application" type
# 7. Add authorized redirect URIs:
#    - For mobile: your app's custom scheme (e.g., com.upcoach.app:/oauth2redirect)
#    - For web: http://localhost:8080/api/v2/auth/google/callback
# 8. Copy the Client ID and Client Secret to this file