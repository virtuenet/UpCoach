# Monitoring Configuration Example
# Copy this file to .env and fill in your actual values

# Sentry Configuration (Error Tracking & Performance Monitoring)
SENTRY_DSN=https://your-dsn@sentry.io/your-project-id
SENTRY_ENVIRONMENT=production
SENTRY_RELEASE=1.0.0
SENTRY_TRACES_SAMPLE_RATE=0.1
SENTRY_PROFILES_SAMPLE_RATE=0.1

# Frontend Sentry (for CMS and Admin panels)
VITE_SENTRY_DSN=https://your-frontend-dsn@sentry.io/your-project-id
VITE_SENTRY_ENVIRONMENT=production
VITE_APP_VERSION=1.0.0

# DataDog APM Configuration
DATADOG_ENABLED=true
DATADOG_AGENT_HOST=localhost
DATADOG_AGENT_PORT=8126
DATADOG_STATSD_HOST=localhost
DATADOG_STATSD_PORT=8125
DATADOG_SERVICE=upcoach-backend
DATADOG_VERSION=1.0.0

# Note: To enable monitoring in production:
# 1. Sign up for Sentry at https://sentry.io
# 2. Create a new project for your backend and frontend
# 3. Copy the DSN values to the appropriate environment variables
# 4. For DataDog, install the DataDog Agent and configure it
# 5. Update the host and port values if DataDog Agent is running elsewhere